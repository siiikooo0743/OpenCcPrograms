function concat(parts, glue)
  return(table.concat(parts, glue))
end

function split(string, seperator)
	ret = {}
	c = 0
	for s in string.gmatch(string, "[^seperator]+") do
		ret[c] = s
		c = c + 1
	end
end
